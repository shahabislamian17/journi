{
  "name": "journi-next",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "cd backend && npx prisma migrate deploy && npx prisma generate && cd .. && next build",
    "start": "next start",
    "postinstall": "cd backend && npm install && npx prisma generate",
    "vercel-build": "cd backend && npx prisma migrate deploy && npx prisma generate && node prisma/seed.js && cd .. && next build",
    "seed": "cd backend && npx prisma db seed"
  },
  "dependencies": {
    "@apollo/client": "^4.0.11",
    "@prisma/client": "^5.7.1",
    "apollo-upload-client": "^19.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "graphql": "^16.12.0",
    "graphql-ws": "^6.0.6",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "next": "^15.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@types/apollo-upload-client": "^19.0.0",
    "prisma": "^5.7.1"
  }
}
